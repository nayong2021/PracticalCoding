#include <stdio.h>

int main()
{
	FILE *fout;
	//aaa
	fout = fopen("output.txt","w");
	fprintf(fout, "Hello World\n"); // Comment
	fclose(fout);
}
#include <stdio.h>

int main()
{
	FILE *fout;
	fout = fopen("output.txt","w");
	fprintf(fout, "Hello World\n"); // Comment
	fclose(fout);
}
#include <stdio.h>

int main()
{
	FILE *fout;
	fout = fopen("output.txt","w");
	fprintf(fout, "Hello World\n"); // Comment
	fclose(fout);
}
